<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.applnfoSystem.dao.BackendUserMapper">
    <select id="login" resultType="cn.applnfoSystem.pojo.BackendUser">
        SELECT * FROM `backend_user` WHERE userCode=#{userCode}
    </select>
    <select id="getAllBackendUser" resultType="cn.applnfoSystem.pojo.AppInfo">

    </select>
</mapper>